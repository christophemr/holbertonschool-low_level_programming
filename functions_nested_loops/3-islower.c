#include <ctype.h>
#include "main.h"

/**
 * _islower - test for lowercase
 *
 * @c: character to verify
 * Return: 1 on success
 */

int _islower(int c)
{
	int letter;
	letter = 'c';

	if (_islower(letter))

	if (c >= 'a' && c <= 'z')
		return (1);

	return (0);

}
